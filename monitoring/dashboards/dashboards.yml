apiVersion: 1

providers:
  - name: 'LLM Metrics'
    orgId: 1
    folder: 'LLM Metrics'
    folderUid: ''
    type: file
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
